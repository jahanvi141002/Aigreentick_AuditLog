spring.application.name=audit

# MongoDB Configuration
spring.data.mongodb.uri=mongodb://localhost:27017/audit_db
# Alternative: You can specify host, port, and database separately
# spring.data.mongodb.host=localhost
# spring.data.mongodb.port=27017
# spring.data.mongodb.database=audit_db

# Server Configuration
server.port=8081

# Enable MongoDB Change Streams (optional, for advanced auditing)
# spring.data.mongodb.auto-index-creation=true
